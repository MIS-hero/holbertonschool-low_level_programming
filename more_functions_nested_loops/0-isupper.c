#include <stdio.h>
#include "main.h"

/**
*  _isupper - checks for uppercase charachter
*
*  @c: int
*
*  Return: Always 0 (Success)
*/
int _isupper(int c)
{
if ((c > 65), (c < 97))
{
return (1);
}
else
{
return (0);
}
}
